# 이미지에 들어가면 안 되는 것들
# - data/ : 로컬 상태 파일. 컨테이너는 DATA_DIR=/data(볼륨)를 쓰므로 이미지에 넣을 이유가 없다
#           (넣어두면 "배포본에 옛 설정이 섞였나?" 하는 혼동만 부른다)
# - .env  : 토큰/비밀번호. 이미지에 구우면 안 된다
node_modules/
data/
.env
.git/
*.log
.DS_Store
